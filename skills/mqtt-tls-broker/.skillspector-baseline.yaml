# SkillSpector baseline - findings suppressed on future scans.
# Every entry is a reviewed false positive on legitimate TLS/PKI code.
# See docs/SUPPRESSION.md.
version: 1
rules: []
fingerprints:
- hash: sha256:9e19e9efe8f52a9c
  rule_id: LP3
  file: SKILL.md
  reason: "Permissions are declared in this skill's frontmatter; the LP3 heuristic does not parse that block. See permissions: in SKILL.md."
- hash: sha256:5e521f3fd57f0a8a
  rule_id: PE2
  file: SKILL.md
  reason: "Hardening, not escalation: doc shows chmod 600 to protect private keys."
- hash: sha256:1474ba6a30182855
  rule_id: PE2
  file: scripts/gen-certs.sh
  reason: "FP: matched in the file-header comment ('Keys are chmod 600'), not an executable command. See the real chmod calls below (separately reviewed)."
- hash: sha256:f3634574c73bd01d
  rule_id: PE2
  file: scripts/gen-certs.sh
  reason: "Hardening, not escalation: chmod 700 restricts the CA directory to owner-only access, protecting the CA private key."
- hash: sha256:b1917d8430350e0b
  rule_id: PE2
  file: scripts/gen-certs.sh
  reason: "Hardening, not escalation: chmod 600 restricts the generated CA private key (ca.key) to owner read/write only."
- hash: sha256:9f5f0d72edf40490
  rule_id: PE2
  file: scripts/gen-certs.sh
  reason: "Hardening, not escalation: chmod 600 restricts each generated broker/client private key to owner read/write only."
- hash: sha256:35321338342e772f
  rule_id: PE3
  file: scripts/gen-certs.sh
  reason: "FP: matched in the file-header comment ('Sources ../../../config.env'), not an executable command. See the real source line below (separately reviewed)."
- hash: sha256:649bbc880c3c074f
  rule_id: PE3
  file: scripts/gen-certs.sh
  reason: "FP, not credential theft: sources the repo's shared config.env only to read PKI_DIR (a local directory path). No other variable from that file (e.g. NEMOTRON_API_KEY, TEAMS_OUTGOING_SECRET) is read, printed, or transmitted; cert generation runs entirely through local openssl calls."
- hash: sha256:8543fadc8b841565
  rule_id: PE3
  file: scripts/verify-tls.sh
  reason: "FP, not exfiltration: reads local cert/key paths to verify the TLS listener; node-local, nothing transmitted."
- hash: sha256:1acd79f4fd51fadd
  rule_id: TM1
  file: scripts/gen-certs.sh
  reason: "FP: rm -f is scoped to three named, script-generated intermediate files (.csr/.ext/.csr.cnf) inside the PKI dir it just created; not a broad glob or -rf, no untrusted input."
