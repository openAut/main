# SkillSpector baseline - findings suppressed on future scans.
# Every entry is a reviewed false positive on legitimate SSH provisioning code.
# See docs/SUPPRESSION.md.
version: 1
rules: []
fingerprints:
- hash: sha256:9e19e9efe8f52a9c
  rule_id: LP3
  file: SKILL.md
  reason: "Permissions are declared in this skill's frontmatter; the LP3 heuristic does not parse that block. See permissions: in SKILL.md."
- hash: sha256:b0133d2366e70eaf
  rule_id: PE3
  file: SKILL.md
  reason: "FP: documentation reference to config.env (node-provisioned). No credential is embedded or transmitted."
- hash: sha256:b3f9e916c2b3e880
  rule_id: PE3
  file: scripts/preflight.sh
  reason: "FP, not exfiltration: sources node-local config.env to read SANDBOX_HOST/SSH_USER for preflight checks; nothing is transmitted off-host."
- hash: sha256:cfeaa312695cee3c
  rule_id: PE3
  file: scripts/preflight.sh
  reason: "FP, not exfiltration: sources node-local config.env to read SANDBOX_HOST/SSH_USER for preflight checks; nothing is transmitted off-host."
- hash: sha256:2f3b0b1419ffd6a9
  rule_id: PE3
  file: scripts/verify-inference.sh
  reason: "FP, not exfiltration: reads NEMOTRON_API_KEY/CA from node-local config.env to authenticate the inference check; used over TLS, never logged or sent elsewhere."
- hash: sha256:47143ddf47469319
  rule_id: TM1
  file: scripts/verify-inference.sh
  reason: "FP: curl uses --cacert to enforce TLS certificate verification and deliberately omits -k/--insecure; not parameter abuse."
