# Local config with host names, webhook URLs and secrets — never commit.
config.env

# PKI: CA + broker + client private keys generated by mqtt-tls-broker/scripts/gen-certs.sh
pki/
*.key
*.pem

# Python
__pycache__/
*.pyc
